const settings = {
  camera: {
    panSpeed: 0.2,
    perspective: 500,
    rotateSpeed: 0.01,
    zoom: {
      max: 4000,
      default: 500,
      min: 100,
    },
    zoomSpeed: 1,
  },
};

export default settings;
